{
  "name": "pci-kpi",
  "version": "1.0.0",
  "private": true,
  "description": "PCI KPI Management — Variable Pay Workflow monorepo",
  "scripts": {
    "build:client": "npm ci --prefix client && npm run build --prefix client",
    "build:api": "npm ci --prefix api --omit=dev",
    "build": "npm run build:client && npm run build:api",
    "start": "node api/server.js",
    "dev:api": "npm run dev --prefix api",
    "dev:client": "npm run dev --prefix client"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "pg": "^8.13.1"
  }
}
